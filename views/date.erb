<!DOCTYPE html>

  <h1>Today</h1>
  <%date = Date.today.strftime("%A, %B %d, %Y")%>
  <p><%= "The date is #{date}"%></p>
  <p><%= "Date.today"%></p>
  <p><%= "Date.now"%></p>
   <!-- The date is Wednesday, November 18, 2015. -->
